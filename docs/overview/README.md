# Overview

The platform-level description of the system: what it is made of and how the pieces fit together, independent of any business domain. Read [architecture](architecture.md) first; every other document in this folder assumes the registry, the environment, the record set and the unit of work it defines.

| Document | Content |
|---|---|
| [`architecture.md`](architecture.md) | Layers, tenancy, process model, the request path, the entity registry, the environment, the record set, the unit of work, generic against named operations, concurrency and retry, cross-process coherence, the error taxonomy |
| [`design-principles.md`](design-principles.md) | The recurring design choices that give the system its behaviour: everything is an entity, the interface is data, configuration is data, external identifiers, archival over deletion, audit fields, company scope per record, currency and precision discipline, extension over modification, declared dependencies — with their costs, their conflicts and the principles deliberately not adopted |
| [`package-system.md`](package-system.md) | Capability packages: manifests and their validation, version normalisation, external prerequisites, discovery, the package record, dependency resolution, installation order, data files and the record declaration grammar, external identifiers, reload semantics, automatic installation, exclusions, the install, update, reinitialise and removal lifecycles, hooks, demonstration data, categories, client asset bundles and the shipped catalogue |
| [`entity-and-field-system.md`](entity-and-field-system.md) | Entity kinds; every field type with its storage, rounding and value semantics; relational fields and the materialised ancestor path; computed fields, their dependencies, recomputation and derived capabilities; related fields; company-dependent values; translatable values; defaults and recorded default values; on-change behaviour; validation; ordering; display names; the archive flag; copying; field descriptions served to clients; how the database schema is derived; user-created fields |
| [`record-operations-and-query-notation.md`](record-operations-and-query-notation.md) | The generic operations every entity supports — create, read, fetch, search, write, delete, grouped reading, naming, duplication, defaults, export and load — with their checks, their order and their errors; and the complete filter notation with every operator, the empty-value rules, the optimisation stages and in-memory evaluation |
| [`inheritance-and-extension.md`](inheritance-and-extension.md) | Extending an entity in place, copying a definition, embedding a parent record and adopting an abstract behaviour; how a resolved definition is composed; extending fields, operations, views, data, security, printable documents and assets; ownership and resolution order |
| [`security-model.md`](security-model.md) | The four data gates and the six layers around them: users, groups, privilege families, access rights, record rules, field restrictions, the checks each generic operation performs, elevated rights, which operations are reachable from outside, authentication levels, sessions, sign-in, signed tokens, external identities, view and menu visibility, caching, the shipped catalogue and the message catalogue |
| [`multi-company.md`](multi-company.md) | One installation serving several legal entities: the company tree and its branches, allowed, activated and current companies, company fields, company scoping through record rules, the company consistency check, per-company values, currency, elevated rights and companies, cross-company flows, what changes at a switch, and the caches keyed by company |
| [`views-and-actions.md`](views-and-actions.md) | The view record and the grammar of every view kind, conditions and group requirements, view inheritance and priority, view resolution and its post-processing passes, validation, the six kinds of action, menus, saved filters, embedded actions, the further kinds contributed by capability packages, the widget catalogue and embedded sub-views |
| [`client-architecture.md`](client-architecture.md) | The desktop client as a contract: bootstrap and the session payload, the navigation tree, the action manager and the navigation trail, the control panel, the search model, loading a screen, the record and table screens, editing lists of related records, paging, notices and dialogs, the top bar, the command palette, keyboard shortcuts, the service catalogue and the offline behaviour of the point of sale client |
| [`messaging-model.md`](messaging-model.md) | The adoptable messaging behaviour: threads, messages, subtypes, followers, notifications, field change tracking, activities, automatic subscription, posting, access to messages and the contracts adoption imposes |
