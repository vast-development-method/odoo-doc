# Qweb field one2many (`ir.qweb.field.one2many`)

**Transport name:** `ir.qweb.field.one2many`  
**Storage name:** `ir_qweb_field_one2many`  
**Kind:** abstract entity (a mixin of fields and operations reused by other entities, no table of its own)  
**Defined by package:** `base`

Description: Qweb field one2many

## Identity and behavior

- Mixins (classical inheritance): `ir.qweb.field`

## Operations (1)

| Operation | Kind | Arguments | Defined in packages | Triggers and dependencies | Documentation |
|---|---|---|---|---|---|
| `value_to_html` | operation | self, value, options | `base` | model |  |

Machine-readable definition: `../../../schemas/data/entities/ir.qweb.field.one2many.json`.
