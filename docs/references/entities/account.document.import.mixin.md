# Business document import mixin (`account.document.import.mixin`)

**Transport name:** `account.document.import.mixin`  
**Storage name:** `account_document_import_mixin`  
**Kind:** abstract entity (a mixin of fields and operations reused by other entities, no table of its own)  
**Defined by package:** `account`

Description: Business document import mixin

## Operations (19)

| Operation | Kind | Arguments | Defined in packages | Triggers and dependencies | Documentation |
|---|---|---|---|---|---|
| `_create_records_from_attachments` | internal rule | self, attachments, grouping_method | `account` | model | For each attachment, create a corresponding record, and attempt to decode the attachment on the record.  Some attachments (e.g. in some EDI formats) may contain multiple business documents; in that case, we attempt to separate them and create a new record for each business document.  ⚠️ Because this method commits the cursor, try to: (1) do as much work as possible before calling this method, and (2) avoid triggering a SerializationError later in the request. If a SerializationError happens,     `retrying` will cause the whole request to be retried, which may cause some things     to be duplic |
| `_group_files_data_by_origin_attachment` | internal rule | self, files_data | `account` |  | A naive grouping method which does the following:  - if a file_data has an 'origin_attachment', it is assigned to the same group as the 'origin_attachment'. - otherwise, it is assigned to a new group. |
| `_group_files_data_into_groups_of_mixed_types` | internal rule | self, files_data | `account` |  | A grouping method with a heuristic that enables it to dispatch files of the same type to different groups, but files of different types to the same group.  This makes it suitable for grouping attachments received through a journal mail alias. For example, receiving 5 PDFs will dispatch them into 5 groups (one per PDF), but receiving one PDF, one JPG and one XML will dispatch them all into a single group. |
| `_assign_attachment_to_group_of_different_type` | internal rule | self, incoming_file_data, groups | `account` |  | Add the attachment to the group which doesn't yet have an attachment of the same root type (however, attachments with no root type don't clash with each other). If several groups are available, we choose the group which has the highest filename similarity. |
| `_assign_attachment_to_group_with_same_origin_attachment` | internal rule | self, incoming_file_data, groups | `account` |  | Attachments that come from the same origin attachment are added to the same group. |
| `_get_similarity_score` | preparation rule | self, filename1, filename2 | `account` |  | Compute a similarity score between two filenames. This is used to group files with similar names together as much as possible when figuring out how to dispatch attachments received in a mail alias.  Similarity is defined as the length of the largest common substring between the two filenames. |
| `_extend_with_attachments` | internal rule | self, files_data, new | `account` |  | Extend/enhance a business document with one or more attachments.  Only the attachment with the highest priority will be used to extend the business document, using the appropriate decoder.  The decoder may break Python and SQL constraints in difficult-to-predict ways. This method calls the decoder in such a way that any exceptions instead roll back the transaction and log a message on the invoice chatter.  This method will not extract embedded files for you - if you want embedded files to be considered, you must pass them as part of the `attachments` recordset.  :param self:        An invoice  |
| `_get_edi_decoder` | preparation rule | self, file_data, new | `account` |  | Main method that should be overridden to implement decoders for various file types.  :param file_data: A dict representing an attachment which should be decoded. :param new:       (optional) whether the business document was newly created. :return:          A dict with the following keys:     - decoder:     The decoder function to use. This function should return either None                    if decoding was successful, or a string explaining why decoding failed.     - priority:    The priority of the decoder. |
| `_attachment_fields_to_clear` | internal rule | self | `account` |  | Return a list of fields that should be cleared when an attachment is unattached from the record. |
| `_fix_attachments_on_record` | internal rule | self, attachments | `account` |  | Ensure that only attachments of certain types appear in `self`'s attachments.  This is to provide a consistent behaviour where only certain attachment types appear in the chatter's attachments, to avoid cluttering the attachments view. |
| `_fix_attachments_on_record_from_files_data` | internal rule | self, valid_files_data, extra_files_data | `account` |  |  |
| `_should_attach_to_record` | internal rule | self, attachment | `account` |  | Indicate whether a given attachment should be displayed in the record's attachments. |
| `_to_files_data` | internal rule | self, attachments | `account` | model | Helper method to convert an ir.attachment recordset into an intermediate `files_data` format used by the import framework.  :return: a list of dicts, each dict representing one of the attachments in `self`. |
| `_from_files_data` | internal rule | self, files_data | `account` | model | Helper method to convert a `files_data` list-of-dicts back into an ir.attachment recordset. This only returns those elements in `files_data` which correspond to an ir.attachment (thus, embedded files that were never turned into ir.attachments are omitted). |
| `_get_import_file_type` | preparation rule | self, file_data | `account` | model | Method to be overridden to identify a file's format. |
| `_get_xml_tree` | preparation rule | self, file_data | `account` | model | Parse file_data['raw'] into an lxml.etree.ElementTree. Can be overridden if custom decoding is needed. |
| `_unwrap_attachments` | internal rule | self, files_data, recurse | `account` | model | Unwrap and return any embedded files.  :param files_data: The files to be unwrapped. :param recurse: if True, embedded-of-embedded attachments will also be unwrapped and returned. :return: a `files_data` list representation of the embedded attachments. |
| `_unwrap_attachment` | internal rule | self, file_data, recurse | `account` | model | Unwrap a single attachment and return its embedded attachments.  This method can be overridden to implement custom unwrapping behaviours (e.g. EDI formats which contain multiple business documents in a single file)  :param file_data: The file to be unwrapped. :param recurse: if True, should return embedded-of-embedded attachments. :return: a `files_data` list representation of the embedded attachements. |
| `_split_xml_into_new_attachments` | internal rule | self, file_data, tag | `account` | model | Helper method to split an XML file into multiple files on a given tag.  In EDIs, some XMLs contain multiple business documents. In such cases, we often want any business document beyond the first to have its own attachment that can be decoded separately. This helper method looks whether the provided XML tree (given in `file_data`) has multiple instances of the given `tag`, and creates a new attachment for each tag beyond the first. The new attachment has the same XML structure as the original file, but only has one instance of the specified tag.  :param file_data: The XML file to split :param  |

Machine-readable definition: `../../../schemas/data/entities/account.document.import.mixin.json`.
