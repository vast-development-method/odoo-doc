# Push Notification Device (`mail.push.device`)

**Transport name:** `mail.push.device`  
**Storage name:** `mail_push_device`  
**Kind:** persistent entity (one table)  
**Defined by package:** `mail`

Description: Push Notification Device

## Identity and behavior

- Audit fields: `id`, `create_date`, `create_uid`, `write_date`, `write_uid`

## Fields (4)

| Field (storage name) | Full name | Type | Target | Meaning and rules |
|---|---|---|---|---|
| `partner_id` | Partner | many to one | `res.partner` | required; default computed dynamically (lambda self: self.env.user.partner_id); indexed |
| `endpoint` | Browser endpoint | single line text |  | required |
| `keys` | Browser keys | single line text |  | required; Help: It's refer to browser keys used by the notification:  - p256dh: It's the subscription public key generated by the browser. The browser will            keep the private key secret and use it for decrypting the payload - auth: The auth value should be treated as a secret and not shared outside of Odoo |
| `expiration_time` | Expiration Token Date | date and time |  |  |

## Database constraints and indexes (1)

| Name | Kind | Definition | Message | Package |
|---|---|---|---|---|
| `_endpoint_unique` | Constraint | `unique(endpoint)` | The endpoint must be unique ! | `mail` |

## Operations (4)

| Operation | Kind | Arguments | Defined in packages | Triggers and dependencies | Documentation |
|---|---|---|---|---|---|
| `get_web_push_vapid_public_key` | operation | self | `mail` | model |  |
| `register_devices` | operation | self, **kw | `mail` | model |  |
| `unregister_devices` | operation | self, **kw | `mail` | model |  |
| `_verify_vapid_public_key` | internal rule | self, sw_public_key | `mail` |  |  |

## Access rights

| Group | Create | Read | Update | Delete | Package |
|---|---|---|---|---|---|
| `base.group_system` | yes | yes | yes | yes | `mail` |

Machine-readable definition: `../../../schemas/data/entities/mail.push.device.json`.
