# Qweb (`ir.qweb`)

**Transport name:** `ir.qweb`  
**Storage name:** `ir_qweb`  
**Kind:** abstract entity (a mixin of fields and operations reused by other entities, no table of its own)  
**Defined by package:** `base`  
**Extended by packages:** `bus`, `html_editor`, `mail`, `http_routing`, `portal`, `im_livechat`, `website`

Description: Qweb

## Operations (76)

| Operation | Kind | Arguments | Defined in packages | Triggers and dependencies | Documentation |
|---|---|---|---|---|---|
| `_render` | internal rule | self, template, values, **options | `base` | model | Render the template specified by the given name.  :param template: etree, xml_id, template name (see _get_template)     * Call the method ``load`` is not an etree. :param dict values: template values to be used for rendering :param options: used to compile the template     Options will be add into the IrQweb.env.context for the rendering.      * ``lang`` (str) used language to render the template     * ``inherit_branding`` (bool) add the tag node branding     * ``inherit_branding_auto`` (bool) add the branding on fields     * ``minimal_qcontext``(bool) To use the minimum context and options    |
| `_render_iterall` | internal rule | self, view_ref, method, values, directive | `base` |  | Iterate over the generator method. Generator elements are a str |
| `_get_error_info` | preparation rule | self, error, stack, frame | `base` |  |  |
| `_get_template_cache_keys` | preparation rule | self | `base`, `html_editor`, `mail`, `website` |  | Return the list of context keys to use for caching ``_compile``. |
| `_get_template_info` | preparation rule | self, template | `base` |  |  |
| `_compile` | internal rule | self, template | `base` |  |  |
| `_generate_code_cached` | internal rule | self, ref | `base` |  |  |
| `_generate_code_uncached` | internal rule | self, template | `base` |  |  |
| `_generate_code` | internal rule | self, template | `base` |  | Compile the given template into a rendering function (generator)::      render_template(qweb, values)  This method can be called only by :meth:`_render` method or by the compiled code of t-call from an other template.  An ``options`` dictionary is created and attached to the function. It contains rendering options that are part of the cache key in addition to template references.  where ``qweb`` is a QWeb instance and ``values`` are the values to render.  :returns: tuple containing code, options and main method name |
| `_get_template` | preparation rule | self, template | `base`, `website` |  | Retrieve the given template, and return it as a tuple ``(etree, xml, ref)``, where ``element`` is an etree, ``document`` is the string document that contains ``element``, and ``ref`` if the uniq reference of the template (id, t-name or template).  :param template: template identifier or etree |
| `_get_preload_attribute_xmlids` | preparation rule | self | `base`, `html_editor` | model |  |
| `_preload_trees` | internal rule | self, refs | `base` |  | Preload all tree and subtree (from t-call and other '_get_preload_attribute_xmlids' values).  Returns::      {         id or xmlId/key: {             'xmlid': str \| None,             'ref': int \| None,             'tree': etree \| None,             'template': str \| None,             'error': None \| MissingError         }     } |
| `_get_converted_image_data_uri` | preparation rule | self, base64_source | `base` |  |  |
| `_prepare_environment` | preparation rule | self, values | `base`, `http_routing` |  | Prepare the values and context that will sent to the compiled and evaluated function.  :param values: template values to be used for rendering  :returns self (with new context) |
| `__prepare_globals` | internal rule | self | `base` |  | Prepare the global context that will sent to eval the qweb generated code. |
| `_append_text` | internal rule | self, text, compile_context | `base` |  | Add an item (converts to a string) to the list. This will be concatenated and added during a call to the `_flush_text` method. This makes it possible to return only one yield containing all the parts. |
| `_rstrip_text` | internal rule | self, compile_context | `base` |  | The text to flush is right stripped, and the stripped content are returned. |
| `_flush_text` | internal rule | self, compile_context, level, rstrip | `base` |  | Concatenate all the textual chunks added by the `_append_text` method into a single yield. If no text to flush, return an empty list  If rstrip the text is right stripped.  @returns list(str) |
| `_is_static_node` | internal rule | self, el, compile_context | `base` |  | Test whether the given element is purely static, i.e. (there are no t-* attributes), does not require dynamic rendering for its attributes. |
| `_compile_format` | internal rule | self, expr | `base` |  | Parses the provided format string and compiles it to a single expression python, uses string with format method. Use format is faster to concat string and values. |
| `_compile_expr_tokens` | internal rule | self, tokens, allowed_keys, argument_names, raise_on_missing | `base` |  | Transform the list of token coming into a python instruction in textual form by adding the namepaces for the dynamic values.  Example: `5 + a + b.c` to be `5 + values.get('a') + values['b'].c` Unknown values are considered to be None, but using `values['b']` gives a clear error message in cases where there is an attribute for example (have a `KeyError: 'b'`, instead of `AttributeError: 'NoneType' object has no attribute 'c'`).  @returns str |
| `_compile_expr` | internal rule | self, expr, raise_on_missing | `base`, `mail` |  | Transform string coming into a python instruction in textual form by adding the namepaces for the dynamic values. This method tokenize the string and call ``_compile_expr_tokens`` method.  :param expr: string: python expression :param bool raise_on_missing:     Compile has `values['product'].price` instead of     `values.get('product').price` to raise an error when get the     'product' value and not an 'NoneType' object has no attribute     'price' error. |
| `_compile_bool` | internal rule | self, attr, default | `base` |  | Convert the statements as a boolean. |
| `_compile_to_str` | internal rule | self, expr | `base` |  | Generates a text value (an instance of text_type) from an arbitrary source. |
| `_directives_eval_order` | internal rule | self | `base`, `html_editor` |  | List all supported directives in the order in which they should be evaluated on a given element. For instance, a node bearing both ``foreach`` and ``if`` should see ``foreach`` executed before ``if`` aka  .. code-block:: xml      <el t-foreach="foo" t-as="bar" t-if="bar">  should be equivalent to  .. code-block:: xml      <t t-foreach="foo" t-as="bar">         <t t-if="bar">             <el>  then this method should return ``['foreach', 'if']``. |
| `_compile_node` | internal rule | self, el, compile_context, level | `base`, `html_editor` |  | Compile the given element into python code.      The t-* attributes (directives) will be converted to a python instruction. If there     are no t-* attributes, the element will be considered static.      Directives are compiled using the order provided by the     ``_directives_eval_order`` method (an create the     ``compile_context['iter_directives']`` iterator).     For compilation, the directives supported are those with a     compilation method ``_compile_directive_*``  :return: list of string |
| `_compile_static_node` | internal rule | self, el, compile_context, level | `base` |  | Compile a purely static element into a list of string. |
| `_compile_directives` | internal rule | self, el, compile_context, level | `base` |  | Compile the given element, following the directives given in the iterator ``compile_context['iter_directives']`` create by `_compile_node`` method.  :return: list of code lines |
| `_compile_directive` | internal rule | self, el, compile_context, directive, level | `base`, `mail` |  |  |
| `_compile_directive_debug` | internal rule | self, el, compile_context, level | `base` |  | Compile `t-debug` expressions into a python code as a list of strings.  The code will contains the call to the debugger chosen from the valid list. |
| `_compile_directive_options` | internal rule | self, el, compile_context, level | `base` |  | compile t-options and add to the dict the t-options-xxx. Will create the dictionary ``values['__qweb_options__']`` in compiled code. |
| `_compile_directive_consumed_options` | internal rule | self, el, compile_context, level | `base` |  |  |
| `_compile_directive_att` | internal rule | self, el, compile_context, level | `base`, `mail` |  | Compile the attributes of the given elements.  The compiled function will create the ``values['__qweb_attrs__']`` dictionary. Then the dictionary will be output.   The new namespaces of the current element.  The static attributes (not prefixed by ``t-``) are add to the dictionary in first.  The dynamic attributes values will be add after. The dynamic attributes has different origins.  - value from key equal to ``t-att``: python dictionary expression; - value from keys that start with ``t-att-``: python expression; - value from keys that start with ``t-attf-``: format string     expression. |
| `_compile_directive_tag_open` | internal rule | self, el, compile_context, level | `base` |  | Compile the opening tag with attributes of the given element into a list of python code line.  The compiled function will fill the ``attrs`` dictionary. Then the ``attrs`` dictionary will be output and reset the value of ``attrs``.  The static attributes (not prefixed by ``t-``) are add to the ``attrs`` dictionary in first.  The dynamic attributes values will be add after. The dynamic attributes has different origins.  - value from key equal to ``t-att``: python dictionary expression; - value from keys that start with ``t-att-``: python expression; - value from keys that start with ``t-attf-`` |
| `_compile_directive_tag_close` | internal rule | self, el, compile_context, level | `base` |  | Compile the closing tag of the given element into string. Returns an empty list because it's use only `_append_text`. |
| `_compile_directive_set` | internal rule | self, el, compile_context, level | `base` |  | Compile `t-set` expressions into a python code as a list of strings.  There are 3 kinds of `t-set`: * `t-value` containing python code; * `t-valuef` containing strings to format; * `t-valuef.translate` containing translated strings to format; * whose value is the content of the tag (being Markup safe).  The code will contain the assignment of the dynamically generated value. |
| `_compile_directive_value` | internal rule | self, el, compile_context, level | `base` |  | Compile `t-value` expressions into a python code as a list of strings.  This method only check if this attributes is on the same node of a  `t-set` attribute. |
| `_compile_directive_valuef` | internal rule | self, el, compile_context, level | `base` |  | Compile `t-valuef` expressions into a python code as a list of strings.  This method only check if this attributes is on the same node of a  `t-set` attribute. |
| `_compile_directive_inner_content` | internal rule | self, el, compile_context, level | `base` |  | Compiles the content of the element (is the technical `t-inner-content` directive created by QWeb) into a python code as a list of strings.  The code will contains the text content of the node or the compliled code from the recursive call of ``_compile_node``. |
| `_compile_directive_if` | internal rule | self, el, compile_context, level | `base` |  | Compile `t-if` expressions into a python code as a list of strings.  The code will contain the condition `if`, `else` and `elif` part that wrap the rest of the compiled code of this element. |
| `_compile_directive_elif` | internal rule | self, el, compile_context, level | `base` |  | Compile `t-elif` expressions into a python code as a list of strings. This method is linked with the `t-if` directive.  Check if this directive is valide, the t-qweb-skip flag and call `t-if` directive |
| `_compile_directive_else` | internal rule | self, el, compile_context, level | `base` |  | Compile `t-else` expressions into a python code as a list of strings. This method is linked with the `t-if` directive.  Check if this directive is valide and add the t-qweb-skip flag. |
| `_compile_directive_groups` | internal rule | self, el, compile_context, level | `base` |  | Compile `t-groups` expressions into a python code as a list of strings.  The code will contain the condition `if self.env.user.has_groups(groups)` part that wrap the rest of the compiled code of this element. |
| `_compile_directive_foreach` | internal rule | self, el, compile_context, level | `base` |  | Compile ``t-foreach`` expressions into a python code as a list of strings.  * ``t-as`` is used to define the key name. * ``t-foreach`` compiled value can be an iterable, an dictionary or a   number.  The code will contain loop ``for`` that wrap the rest of the compiled code of this element.  Some key into values dictionary are create automatically::      *_size, *_index, *_value, *_first, *_last, *_odd, *_even, *_parity |
| `_compile_directive_as` | internal rule | self, el, compile_context, level | `base` |  | Compile `t-as` expressions into a python code as a list of strings.  This method only check if this attributes is on the same node of a  `t-foreach` attribute. |
| `_compile_directive_out` | internal rule | self, el, compile_context, level | `base`, `mail` |  | Compile `t-out` expressions into a python code as a list of strings.  The code will contain evalution and rendering of the compiled value. If the compiled value is None or False, the tag is not added to the render (Except if the widget forces rendering or there is default content). (eg: `<t t-out="my_value">Default content if falsy</t>`)  The output can have some rendering option with `t-options-widget` or `t-options={'widget': ...}. At rendering time, The compiled code will call ``_get_widget`` method or ``_get_field`` method for `t-field`.  A `t-field` will necessarily be linked to the value |
| `_compile_directive_esc` | internal rule | self, el, compile_context, level | `base` |  |  |
| `_compile_directive_raw` | internal rule | self, el, compile_context, level | `base` |  |  |
| `_compile_directive_field` | internal rule | self, el, compile_context, level | `base` |  | Compile ``t-field`` expressions into a python code as a list of strings.  The compiled code will call ``_get_field`` method at rendering time using the type of value supplied by the field. This behavior can be changed with ``t-options-widget`` or ``t-options={'widget': ...}``.  The code will contain evalution and rendering of the compiled value value from the record field. If the compiled value is None or False, the tag is not added to the render (Except if the widget forces rendering or there is default content.). |
| `_compile_directive_call` | internal rule | self, el, compile_context, level | `base` |  | Compile `t-call` expressions into a python code as a list of strings.  `t-call` allow formating string dynamic at rendering time. Can use `t-options` used to call and render the sub-template at rendering time. The sub-template is called with a copy of the rendering values dictionary. The dictionary contains the key 0 coming from the compilation of the contents of this element  The code will contain the call of the template and a function from the compilation of the content of this element. |
| `_compile_directive_lang` | internal rule | self, el, compile_context, level | `base` |  |  |
| `_compile_directive_call_assets` | internal rule | self, el, compile_context, level | `base` |  | This special 't-call-assets' tag can be used in order to aggregate/minify javascript and css assets |
| `_debug_trace` | internal rule | self, debugger, values | `base` |  | Method called at running time to load debugger. |
| `_post_processing_att` | internal rule | self, tagName, atts | `base`, `website` |  | Method called at compile time for the static node and called at runing time for the dynamic attributes.  This method may be overwrited to filter or modify the attributes (during compilation for static node or after they compilation in the case of dynamic elements).  @returns dict |
| `_get_field` | preparation rule | self, record, field_name, expression, tagName, field_options, values | `base` |  | Method called at compile time to return the field value.  :returns: tuple:     * dict: attributes     * string or None: content     * boolean: force_display display the tag if the content and default_content are None |
| `_get_widget` | preparation rule | self, value, expression, tagName, field_options, values | `base` |  | Method called at compile time to return the widget value.  :returns: tuple:     * dict: attributes     * string or None: content     * boolean: force_display display the tag if the content and default_content are None |
| `_get_asset_nodes` | preparation rule | self, bundle, css, js, debug, defer_load, lazy_load, media, autoprefix | `base` |  | Generates asset nodes. If debug=assets, the assets will be regenerated when a file which composes them has been modified. Else, the assets will be generated only once and then stored in cache. |
| `_get_asset_links` | preparation rule | self, bundle, css, js, debug, autoprefix | `base` |  | Generates asset nodes. If debug=assets, the assets will be regenerated when a file which composes them has been modified. Else, the assets will be generated only once and then stored in cache. |
| `_generate_asset_links_cache` | internal rule | self, bundle, css, js, assets_params, rtl, autoprefix | `base` |  |  |
| `_get_asset_content` | preparation rule | self, bundle, assets_params | `base` |  |  |
| `_get_asset_bundle` | preparation rule | self, bundle_name, css, js, debug_assets, rtl, assets_params, autoprefix | `base` |  |  |
| `_links_to_nodes` | internal rule | self, paths, defer_load, lazy_load, media | `base` |  |  |
| `_link_to_node` | internal rule | self, path, defer_load, lazy_load, media | `base` |  |  |
| `_generate_asset_links` | internal rule | self, bundle, css, js, debug_assets, assets_params, rtl, autoprefix | `base` |  |  |
| `_get_asset_link_urls` | preparation rule | self, bundle, debug | `base` |  |  |
| `_pregenerate_assets_bundles` | internal rule | self | `base` |  | Pregenerates all assets that may be used in web pages to speedup first loading. This may is mainly usefull for tests.  The current version is looking for all t-call-assets in view to generate the minimal set of bundles to generate.  Current version only generate assets without extra, not taking care of rtl. |
| `_get_bundles_to_pregenarate` | preparation rule | self | `base`, `bus`, `im_livechat`, `website` |  | Returns the list of bundles to pregenerate. |
| `_get_bundles_from_views` | preparation rule | self | `base` |  |  |
| `_get_lazy_bundles_from_js` | preparation rule | self | `base` |  |  |
| `_compile_directive_snippet` | internal rule | self, el, compile_context, indent | `html_editor` |  |  |
| `_compile_directive_snippet_call` | internal rule | self, el, compile_context, indent | `html_editor` |  |  |
| `_compile_directive_install` | internal rule | self, el, compile_context, indent | `html_editor` |  |  |
| `_compile_directive_placeholder` | internal rule | self, el, compile_context, indent | `html_editor` |  |  |
| `_is_expression_allowed` | internal rule | self, expression, model | `mail` |  |  |
| `_prepare_frontend_environment` | preparation rule | self, values | `http_routing`, `portal`, `website` |  | Returns ir.qweb with context and update values with portal specific value (required to render portal layout template) |
| `_adapt_style_background_image` | internal rule | self, atts, url_adapter | `website` |  |  |

Machine-readable definition: `../../../schemas/data/entities/ir.qweb.json`.
